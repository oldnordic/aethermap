---
phase: 14-gamepad-emulation-mode
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - aethermap/aethermapd/src/gamepad_device.rs
  - aethermap/aethermapd/src/lib.rs
  - aethermap/aethermapd/Cargo.toml
autonomous: true

must_haves:
  truths:
    - "Virtual Xbox 360 gamepad device can be created via uinput"
    - "Device appears in /dev/input/ as js<N> or event<N>"
    - "Games can detect the virtual gamepad via standard Linux input subsystem"
    - "Device supports ABS_X, ABS_Y axes for left analog stick"
  artifacts:
    - path: "aethermap/aethermapd/src/gamepad_device.rs"
      provides: "Virtual gamepad device creation and management"
      min_lines: 200
      exports: ["GamepadVirtualDevice", "GamepadAxis", "create_xbox360_gamepad"]
    - path: "aethermap/aethermapd/src/lib.rs"
      provides: "Module export for gamepad_device"
      contains: "pub mod gamepad_device"
  key_links:
    - from: "gamepad_device.rs"
      to: "/dev/uinput"
      via: "libc::ioctl calls with UI_SET_EVBIT, UI_SET_ABSBIT"
      pattern: "libc::ioctl.*UI_SET_ABSBIT"
    - from: "GamepadVirtualDevice::emit_axis"
      to: "uinput_fd"
      via: "write() system call with input_event"
      pattern: "write.*uinput_fd.*InputEvent"
---

<objective>
Create virtual Xbox 360 gamepad device module with uinput backend

Purpose: Establish the foundation for gamepad emulation by creating a virtual device that the Linux kernel recognizes as an Xbox 360 controller. This device will receive analog stick events and expose them to games and applications.

Output: New gamepad_device.rs module with GamepadVirtualDevice struct for creating and managing virtual gamepads, integrated into the daemon's module system.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-analog-processing-and-calibration/11-02-SUMMARY.md
@aethermap/aethermapd/src/injector.rs
@aethermap/aethermapd/src/analog_calibration.rs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create gamepad_device.rs module with Xbox 360 device definition</name>
  <files>aethermap/aethermapd/src/gamepad_device.rs</files>
  <action>
Create new module file `aethermap/aethermapd/src/gamepad_device.rs` with:

1. **GamepadAxis enum** - ABS_X (0x00), ABS_Y (0x01), ABS_Z (0x02), ABS_RX (0x03), ABS_RY (0x04), ABS_RZ (0x05) using u16 constants from evdev

2. **GamepadVirtualDevice struct** with:
   - `uinput_fd: Arc<RwLock<Option<RawFd>>>` for thread-safe file descriptor access
   - `device_name: String` for device identification (default "Aethermap Virtual Gamepad")
   - `vendor_id: u16` (default 0x045e = Microsoft)
   - `product_id: u16` (default 0x028e = Xbox 360 Controller)

3. **Constants** for uinput ioctls (reuse from injector.rs):
   - UI_SET_EVBIT, UI_SET_ABSBIT, UI_DEV_CREATE, UI_DEV_DESTROY
   - EV_ABS = 0x03 for absolute axis events

4. **InputEvent struct** (same as injector.rs): `[repr(C)] struct InputEvent { time: timeval, type_: u16, code: u16, value: i32 }`

5. **UinputUserDev struct** for device setup (same as injector.rs but with absmin/absmax configured for gamepad axes):
   - name: [u8; 80]
   - id: InputId (bustype=BUS_USB, vendor=0x045e, product=0x028e, version=0x0110)
   - absmax/absmin arrays for axis ranges (-32768 to 32767)

Use `#[allow(dead_code)]` for structs that will be used in subsequent plans. All uinput constants should match the existing injector.rs pattern.
</action>
  <verify>
```bash
cargo check -p aethermapd --lib 2>&1 | grep -E "(error|warning|gamepad)" || echo "No errors found"
```
</verify>
  <done>Module compiles without errors, Xbox 360 device ID constants defined, uinput structures match injector.rs pattern</done>
</task>

<task type="auto">
  <name>Task 2: Implement GamepadVirtualDevice::new() and create() methods</name>
  <files>aethermap/aethermapd/src/gamepad_device.rs</files>
  <action>
Add to GamepadVirtualDevice:

1. **new() method** - Creates instance with default Xbox 360 IDs:
   ```rust
   pub fn new() -> Self {
       Self {
           uinput_fd: Arc::new(RwLock::new(None)),
           device_name: "Aethermap Virtual Gamepad".to_string(),
           vendor_id: 0x045e,  // Microsoft
           product_id: 0x028e, // Xbox 360 Controller
       }
   }
   ```

2. **create() method** - Opens /dev/uinput and configures device:
   - Open `/dev/uinput` with write-only access (returns error if fails)
   - Enable EV_ABS event type via `UI_SET_EVBIT` ioctl
   - Enable ABS_X, ABS_Y axes via `UI_SET_ABSBIT` ioctl (codes 0x00, 0x01)
   - Configure absmin/absmax in UinputUserDev: absmin[ABS_X] = absmin[ABS_Y] = -32768, absmax[ABS_X] = absmax[ABS_Y] = 32767
   - Write UinputUserDev structure to uinput fd
   - Call `UI_DEV_CREATE` ioctl to create device
   - Store fd in `uinput_fd` field

Follow the exact pattern from UinputInjector::initialize() in injector.rs. Use unsafe block for all ioctl calls. Return `Result<(), Box<dyn std::error::Error + Send + Sync>>`.

Error handling: Use `map_err()` with descriptive error messages for each failure point (uinput open, ioctl, write).
</action>
  <verify>
```bash
cargo check -p aethermapd --lib 2>&1 | head -20
```
</verify>
  <done>create() method compiles, follows UinputInjector pattern, enables EV_ABS with ABS_X and ABS_Y axes</done>
</task>

<task type="auto">
  <name>Task 3: Implement emit_axis() for gamepad axis output</name>
  <files>aethermap/aethermapd/src/gamepad_device.rs</files>
  <action>
Add to GamepadVirtualDevice:

1. **emit_axis() method** with signature:
   ```rust
   pub async fn emit_axis(&self, axis: GamepadAxis, value: i32) -> Result<(), Box<dyn std::error::Error + Send + Sync>>
   ```

2. Implementation:
   - Get uinput_fd from RwLock, return error if None
   - Get current time via `libc::clock_gettime(CLOCK_MONOTONIC, &mut ts)` or use `libc::gettimeofday(&mut tv, ptr::null_mut())`
   - Create InputEvent with:
     - time: current timeval
     - type_: EV_ABS (0x03)
     - code: axis as u16 (ABS_X=0, ABS_Y=1)
     - value: passed i32 value (-32768 to 32767)
   - Write InputEvent to uinput_fd via `libc::write()`
   - Create sync event (EV_SYN=0, SYN_REPORT=0, value=0) and write it
   - Return Ok(())

Error handling: Check write() return value, return error if write failed.

Add #[cfg(test)] module with basic test that GamepadVirtualDevice::new() creates instance.
</action>
  <verify>
```bash
cargo test -p aethermapd lib::gamepad_device 2>&1 | grep -E "(test|running|passed|FAILED)" || echo "Tests ran"
```
</verify>
  <done>emit_axis() writes EV_ABS events to uinput with proper sync, unit test verifies creation</done>
</task>

<task type="auto">
  <name>Task 4: Add module export to lib.rs and verify compilation</name>
  <files>aethermap/aethermapd/src/lib.rs</files>
  <action>
Add to aethermap/aethermapd/src/lib.rs:

1. Add module declaration after other modules:
   ```rust
   pub mod gamepad_device;
   ```

2. Export key types for use in other modules:
   ```rust
   pub use gamepad_device::{GamepadVirtualDevice, GamepadAxis};
   ```

3. No changes to Cargo.toml needed - this is an internal module

Run `cargo check -p aethermapd --lib` to verify compilation.

If there are unused warnings, use `#[allow(dead_code)]` on items that will be used in plans 14-03+.
</action>
  <verify>
```bash
cargo check -p aethermapd --lib 2>&1 | grep -E "Finished|error" | head -5
```
</verify>
  <done>Module exported successfully, cargo check passes without errors, GamepadVirtualDevice available for use</done>
</task>

</tasks>

<verification>
- [ ] Module compiles without errors or warnings
- [ ] Xbox 360 vendor/product IDs match specification (0x045e:0x028e)
- [ ] uinput device creation follows UinputInjector pattern
- [ ] ABS_X and ABS_Y axes enabled with proper range (-32768 to 32767)
- [ ] emit_axis() writes events with sync
- [ ] Unit test for GamepadVirtualDevice::new() passes
</verification>

<success_criteria>
Virtual Xbox 360 gamepad device can be instantiated and configured via uinput. The device structure is ready to receive axis events in subsequent plans.
</success_criteria>

<output>
After completion, create `.planning/phases/14-gamepad-emulation-mode/14-01-SUMMARY.md` with:
- Files created/modified
- Xbox 360 device IDs used
- uinput configuration details
- Any deviations from plan
</output>
