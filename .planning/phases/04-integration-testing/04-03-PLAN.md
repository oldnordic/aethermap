---
phase: 04-integration-testing
plan: 03
type: execute
wave: 2
depends_on:
  - 04-02
files_modified: []
autonomous: true
user_setup: []

must_haves:
  truths:
    - "Hotplug tests cover DeviceEvent::Add and DeviceEvent::Remove variants"
    - "Hotplug tests verify device ID format (VID:PID hex with lowercase)"
    - "Config reload tests verify atomic swap (validate-then-swap pattern)"
    - "Config reload tests verify invalid config rejection without affecting active config"
    - "Config reload tests verify concurrent reload safety"
    - "All hotplug and reload integration tests pass"
  artifacts:
    - path: "aethermap/aethermapd/tests/hotplug_test.rs"
      provides: "188 lines of hotplug event handling tests"
      covered_by: "Task 1"
      exports: ["test_format_device_id", "test_device_event_add", "test_device_event_remove", "test_device_id_format_consistency", "test_device_event_completeness", "test_device_event_debug"]
      min_lines: 187
    - path: "aethermap/aethermapd/tests/config_reload_test.rs"
      provides: "513 lines of configuration hot-reload tests"
      covered_by: "Task 2"
      exports: ["test_valid_remap_reload", "test_invalid_remap_rejection", "test_reload_remaps_with_engine", "test_reload_remaps_validates_keys", "test_valid_device_profile_reload", "test_invalid_device_profile_rejection", "test_device_profile_atomic_swap", "test_empty_remap_reload", "test_concurrent_reload_safety"]
      min_lines: 512
  key_links:
    - from: "hotplug_test.rs"
      to: "aethermapd::hotplug module"
      via: "Testing DeviceEvent and format_device_id exports"
      pattern: "use aethermapd::hotplug::"
    - from: "config_reload_test.rs"
      to: "aethermapd::config::ConfigManager"
      via: "Testing load_remaps(), reload_remaps(), reload_device_profiles()"
      pattern: "manager\\.(load_remaps|reload_remaps|reload_device_profiles)\\(\\)"
    - from: "config_reload_test.rs"
      to: "aethermapd::remap_engine::RemapEngine"
      via: "Testing RemapEngine integration with config reload"
      pattern: "Arc::new\\(RemapEngine::new\\(\\)\\)"
---

<objective>
Verify existing hotplug and configuration reload integration tests provide comprehensive coverage.

Purpose: Research confirms 188 lines of hotplug tests (hotplug_test.rs) and 513 lines of config reload tests (config_reload_test.rs) already exist. This plan verifies those tests cover all Phase 4 success criteria requirements: hotplug scenarios (device removal, re-acquisition) and configuration reload scenarios (atomic swap, invalid rejection).

Output: Verification report confirming test coverage meets Phase 4 success criteria #3 and #4.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04-integration-testing/04-RESEARCH.md

# Existing integration tests
@aethermap/aethermapd/tests/hotplug_test.rs
@aethermap/aethermapd/tests/config_reload_test.rs

# Components under test
@aethermap/aethermapd/src/hotplug.rs
@aethermap/aethermapd/src/config.rs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify hotplug test coverage</name>
  <files>aethermap/aethermapd/tests/hotplug_test.rs</files>
  <action>
    Verify existing hotplug tests (188 lines) cover:
    1. Device ID formatting (test_format_device_id):
       - Lowercase hexadecimal output (ABCD -> abcd)
       - 4-digit zero-padding (0001:0002)
       - Correct separator (:)
    2. DeviceEvent::Add variant (test_device_event_add):
       - Event construction with devnode and device_id
       - Pattern matching extracts fields correctly
       - Clone works for event handling
    3. DeviceEvent::Remove variant (test_device_event_remove):
       - Event construction with devnode and device_id
       - Pattern matching extracts fields correctly
       - Clone works for event handling
    4. Device ID format consistency (test_device_id_format_consistency):
       - Matches YAML config format (vendor:product hex strings)
    5. DeviceEvent completeness (test_device_event_completeness):
       - devnode present and starts with /dev/
       - device_id present and contains separator
    6. Debug representation (test_device_event_debug):
       - Useful logging output for hotplug events

    Run tests: `cargo test -p aethermapd --test hotplug_test`
    All tests should pass.

    No modifications needed - existing tests cover hotplug scenarios.
  </action>
  <verify>cargo test -p aethermapd --test hotplug_test 2>&1 | tail -20</verify>
  <done>All 6 hotplug tests pass, covering DeviceEvent structure, device ID formatting, and event completeness.</done>
</task>

<task type="auto">
  <name>Task 2: Verify config reload test coverage</name>
  <files>aethermap/aethermapd/tests/config_reload_test.rs</files>
  <action>
    Verify existing config reload tests (513 lines) cover:
    1. Valid config reload (test_valid_remap_reload):
       - Initial config loads successfully
       - Updated config loads successfully
       - New remappings are active
    2. Invalid config rejection (test_invalid_remap_rejection):
       - Invalid config is rejected
       - Original config remains active (atomic swap)
    3. RemapEngine integration (test_reload_remaps_with_engine):
       - reload_remaps() updates RemapEngine correctly
    4. Key validation (test_reload_remaps_validates_keys):
       - Invalid key names are rejected
       - Engine keeps original config on validation failure
    5. Device profile reload (test_valid_device_profile_reload):
       - Valid device profiles load successfully
       - Profiles are accessible
    6. Invalid profile rejection (test_invalid_device_profile_rejection):
       - Invalid profiles are rejected
       - Original profiles remain active
    7. Atomic swap (test_device_profile_atomic_swap):
       - Partial invalid configs don't partially apply
       - All-or-nothing validation
    8. Empty config handling (test_empty_remap_reload):
       - Empty config clears remappings without errors
    9. Concurrent safety (test_concurrent_reload_safety):
       - Multiple concurrent reloads don't cause race conditions
       - Final state is consistent

    Run tests: `cargo test -p aethermapd --test config_reload_test`
    All tests should pass.

    No modifications needed - existing tests comprehensively cover reload scenarios.
  </action>
  <verify>cargo test -p aethermapd --test config_reload_test 2>&1 | tail -20</verify>
  <done>All 9 config reload tests pass, covering atomic swap, invalid rejection, concurrent safety, and profile switching.</done>
</task>

<task type="auto">
  <name>Task 3: Run full test suite and document results</name>
  <files>.planning/phases/04-integration-testing/04-03-SUMMARY.md</files>
  <action>
    Run the full test suite to verify all integration tests pass:
    ```bash
    cargo test -p aethermapd --tests
    ```

    Create SUMMARY.md documenting verification results. Include:
    1. Hotplug tests: 6 tests, 188 lines - cover DeviceEvent structure and device ID formatting
    2. Config reload tests: 9 tests, 513 lines - cover atomic swap, invalid rejection, concurrent safety
    3. Total integration tests: 15 tests, 701 lines
    4. All tests pass: cargo test output
    5. Verification that Phase 4 success criteria #3 and #4 (hotplug and config reload scenarios) are satisfied
    6. Note: Hotplug tests cover the API contracts; actual device add/remove requires real hardware or mock udev
    7. Config reload tests use TempDir for isolated test environments
    8. Tests follow existing patterns: tokio::test for async, Arc<RwLock<T>> for thread safety

    Template: use ~/.claude/get-shit-done/templates/summary.md
  </action>
  <verify>cargo test -p aethermapd --tests 2>&1 | grep -E "test result|running|passed"</verify>
  <done>SUMMARY.md created with verification that 15 integration tests (701 lines) cover hotplug and config reload scenarios.</done>
</task>

</tasks>

<verification>
Overall verification:
1. `cargo test -p aethermapd --test hotplug_test` passes (6 tests)
2. `cargo test -p aethermapd --test config_reload_test` passes (9 tests)
3. `cargo test -p aethermapd --tests` passes (all integration tests)
4. Hotplug tests verify: DeviceEvent structure, device ID formatting, event completeness
5. Config reload tests verify: atomic swap, invalid rejection, concurrent safety, profile switching
6. Phase 4 success criteria #3 and #4 satisfied
</verification>

<success_criteria>
1. All 6 hotplug tests pass
2. All 9 config reload tests pass
3. Full integration test suite passes (15 tests total)
4. Test coverage verified against Phase 4 success criteria
5. SUMMARY.md documents verification results
</success_criteria>

<output>
After completion, create `.planning/phases/04-integration-testing/04-03-SUMMARY.md`
</output>
