---
phase: 04-integration-testing
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: []
autonomous: true
user_setup: []

must_haves:
  truths:
    - "Existing RemapEngine unit tests cover key code lookup, modifier handling, and repeat events"
    - "Tests verify eager validation behavior (no partial load on invalid config)"
    - "Tests verify RemapProfile atomic switching via Arc cloning"
    - "All unit tests pass with cargo test"
  artifacts:
    - path: "aethermap/aethermapd/src/remap_engine.rs"
      provides: "251 lines of comprehensive unit tests (lines 439-689)"
      covered_by: "Task 1"
      exports: ["test_remap_engine_creation", "test_load_config_basic", "test_load_config_with_friendly_names", "test_remap_returns_correct_key", "test_invalid_input_key_fails_validation", "test_invalid_output_key_fails_validation", "test_eager_validation_no_partial_load", "test_case_insensitive_config", "test_has_remap", "test_clear_remaps", "test_complex_remap_scenario", "test_shared_key_parser", "test_get_remaps", "test_remap_to_none_for_unmapped_keys", "test_remap_profile_creation", "test_remap_profile_invalid_key_fails", "test_remap_profile_arc_cloning"]
      min_lines: 251
    - path: "aethermap/aethermapd/src/key_parser.rs"
      provides: "235 lines of comprehensive unit tests (lines 308-542)"
      covered_by: "Task 2"
      exports: ["test_letter_parsing", "test_number_parsing", "test_modifier_keys", "test_special_keys", "test_lock_keys", "test_function_keys", "test_arrow_keys", "test_navigation_keys", "test_keypad_keys", "test_unknown_key_returns_error", "test_empty_string_returns_error", "test_at_least_30_keys_mapped", "test_win_meta_super_command_aliases", "test_punctuation_keys"]
      min_lines: 235
  key_links:
    - from: "remap_engine.rs tests"
      to: "evdev::Key enum"
      via: "Direct Key enum usage in test assertions"
      pattern: "assert_eq!\\(.*Key::"
    - from: "remap_engine.rs tests"
      to: "RemapEngine public API"
      via: "Testing load_config(), remap(), process_event(), clear()"
      pattern: "engine\\.(load_config|remap|process_event|clear)\\(\\)"
---

<objective>
Verify existing RemapEngine unit tests provide comprehensive coverage of core remapping logic.

Purpose: Research confirms 251 lines of RemapEngine tests (17 tests) and 235 lines of KeyParser tests (15 tests) already exist. This plan verifies those tests cover all required functionality: key code lookup, modifier handling, repeat events, eager validation, and RemapProfile atomic switching.

Output: Verification report confirming test coverage meets Phase 4 success criteria for unit tests.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04-integration-testing/04-RESEARCH.md

# Existing test coverage verified from source
@aethermap/aethermapd/src/remap_engine.rs
@aethermap/aethermapd/src/key_parser.rs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify RemapEngine test coverage</name>
  <files>aethermap/aethermapd/src/remap_engine.rs</files>
  <action>
    Verify existing RemapEngine unit tests (lines 439-689, 251 lines) cover:
    1. Engine creation and default state (test_remap_engine_creation)
    2. Basic config loading (test_load_config_basic)
    3. Friendly key name support (test_load_config_with_friendly_names)
    4. Key lookup and remapping (test_remap_returns_correct_key)
    5. Input key validation (test_invalid_input_key_fails_validation)
    6. Output key validation (test_invalid_output_key_fails_validation)
    7. Eager validation - no partial load (test_eager_validation_no_partial_load)
    8. Case-insensitive config parsing (test_case_insensitive_config)
    9. has_remap() query method (test_has_remap)
    10. clear() method (test_clear_remaps)
    11. Complex remap scenarios (test_complex_remap_scenario)
    12. Shared KeyParser support (test_shared_key_parser)
    13. get_remaps() inspection (test_get_remaps)
    14. Unmapped key behavior (test_remap_to_none_for_unmapped_keys)
    15. RemapProfile creation (test_remap_profile_creation)
    16. RemapProfile validation (test_remap_profile_invalid_key_fails)
    17. RemapProfile Arc cloning (test_remap_profile_arc_cloning)

    Run tests: `cargo test -p aethermapd --lib remap_engine::tests`
    All tests should pass.

    No modifications needed - existing tests are comprehensive.
  </action>
  <verify>cargo test -p aethermapd --lib remap_engine::tests 2>&1 | tail -20</verify>
  <done>All 17 RemapEngine unit tests pass, covering lookup tables, validation, and profile switching.</done>
</task>

<task type="auto">
  <name>Task 2: Verify KeyParser test coverage</name>
  <files>aethermap/aethermapd/src/key_parser.rs</files>
  <action>
    Verify existing KeyParser unit tests (lines 308-542, 235 lines) cover:
    1. Letter key parsing (test_letter_parsing)
    2. Number key parsing (test_number_parsing)
    3. Modifier key handling (test_modifier_keys)
    4. Special key parsing (test_special_keys)
    5. Lock key handling (test_lock_keys)
    6. Function key parsing (test_function_keys)
    7. Arrow key parsing (test_arrow_keys)
    8. Navigation key parsing (test_navigation_keys)
    9. Keypad key parsing (test_keypad_keys)
    10. Unknown key error handling (test_unknown_key_returns_error)
    11. Empty string error handling (test_empty_string_returns_error)
    12. Minimum key coverage (test_at_least_30_keys_mapped)
    13. Win/Meta/Super aliases (test_win_meta_super_command_aliases)
    14. Punctuation key parsing (test_punctuation_keys)
    15. Additional coverage tests

    Run tests: `cargo test -p aethermapd --lib key_parser::tests`
    All tests should pass.

    No modifications needed - existing tests cover all key categories.
  </action>
  <verify>cargo test -p aethermapd --lib key_parser::tests 2>&1 | tail -20</verify>
  <done>All KeyParser unit tests pass, covering letter, number, modifier, special, function, arrow, navigation, keypad, and punctuation keys.</done>
</task>

<task type="auto">
  <name>Task 3: Document test coverage verification</name>
  <files>.planning/phases/04-integration-testing/04-01-SUMMARY.md</files>
  <action>
    Create SUMMARY.md documenting test coverage verification. Include:
    1. RemapEngine: 17 tests, 251 lines - covers lookup, validation, repeat handling, profiles
    2. KeyParser: 15 tests, 235 lines - covers all key categories (letter, number, modifier, special, function, arrow, navigation, keypad, punctuation)
    3. All tests pass: `cargo test -p aethermapd --lib` output
    4. Verification that Phase 4 success criterion #1 (unit tests cover core remapping logic) is already satisfied

    Template: use ~/.claude/get-shit-done/templates/summary.md
  </action>
  <verify>cat .planning/phases/04-integration-testing/04-01-SUMMARY.md | grep -E "tests|coverage|RemapEngine|KeyParser"</verify>
  <done>SUMMARY.md created with verification that 32 unit tests (486 lines) already provide comprehensive coverage.</done>
</task>

</tasks>

<verification>
Overall verification:
1. `cargo test -p aethermapd --lib` passes with all 32 unit tests passing
2. RemapEngine tests cover: lookup tables, modifier handling, repeat events, eager validation, profile switching
3. KeyParser tests cover: all key categories including letters, numbers, modifiers, special keys, functions, arrows, navigation, keypad, punctuation
4. No gaps identified in existing unit test coverage
</verification>

<success_criteria>
1. All existing RemapEngine unit tests (17 tests) pass
2. All existing KeyParser unit tests (15 tests) pass
3. Test coverage verified against Phase 4 success criteria
4. SUMMARY.md documents verification results
</success_criteria>

<output>
After completion, create `.planning/phases/04-integration-testing/04-01-SUMMARY.md`
</output>
