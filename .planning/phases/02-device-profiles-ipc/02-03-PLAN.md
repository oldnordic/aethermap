---
phase: 02-device-profiles-ipc
plan: 03
type: execute
wave: 2
depends_on: [02-01]
files_modified:
  - razermapper/razermapper-common/src/lib.rs
  - razermapper/razermapperd/src/ipc.rs
autonomous: true

must_haves:
  truths:
    - "GUI can query which remap profile is active for a device"
    - "GUI can request list of available profiles for a device"
    - "GUI can activate or deactivate a remap profile via IPC"
  artifacts:
    - path: "razermapper/razermapper-common/src/lib.rs"
      provides: "IPC protocol extensions for profile operations"
      exports: ["Request::GetActiveRemaps", "Request::ListRemapProfiles", "Request::ActivateRemapProfile", "Request::DeactivateRemapProfile"]
      covered_by: "Task 1: Extend IPC Request/Response enums"
    - path: "razermapper/razermapperd/src/ipc.rs"
      provides: "IPC handler scaffolds for profile operations"
      exports: ["Request handlers"]
      covered_by: "Task 2: Add IPC handler scaffolds"
  key_links:
    - from: "razermapper/razermapper-common/src/lib.rs"
      to: "razermapper/razermapperd/src/ipc.rs"
      via: "Request/Response enum matching"
      pattern: "Request::(GetActiveRemaps|ListRemapProfiles|ActivateRemapProfile|DeactivateRemapProfile)"
---

<objective>
Extend IPC protocol with request/response types for querying active remaps and switching profiles.

Purpose: The GUI needs to query active remaps, list available profiles, and activate/deactivate profiles at runtime. This plan adds the IPC protocol definitions for these operations.

Output: Extended Request/Response enums in razermapper-common and IPC handler scaffolds in ipc.rs.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-device-profiles-ipc/02-RESEARCH.md

# Existing IPC patterns
@razermapper/razermapper-common/src/lib.rs
@razermapper/razermapperd/src/ipc.rs
</context>

<tasks>

<task type="auto">
  <name>Extend IPC Request/Response enums</name>
  <files>razermapper/razermapper-common/src/lib.rs</files>
  <action>
    Update razermapper/razermapper-common/src/lib.rs:

    1. Add RemapProfileInfo struct (after existing structs):
       ```rust
       /// Information about a remap profile for listing
       #[derive(Debug, Clone, Serialize, Deserialize)]
       pub struct RemapProfileInfo {
           /// Profile name
           pub name: String,
           /// Human-readable description
           pub description: Option<String>,
           /// Number of remaps in this profile
           pub remap_count: usize,
       }
       ```

    2. Add Request enum variants (in existing Request enum):
       ```rust
       /// Query active remap configuration for a device
       GetActiveRemaps {
           device_path: String,
       },

       /// List available remap profiles for a device
       ListRemapProfiles {
           device_path: String,
       },

       /// Activate a remap profile for a device
       ActivateRemapProfile {
           device_path: String,
           profile_name: String,
       },

       /// Deactivate current remap profile for a device
       DeactivateRemapProfile {
           device_path: String,
       },
       ```

    3. Add Response enum variants (in existing Response enum):
       ```rust
       /// Active remap configuration
       ActiveRemaps {
           device_path: String,
           profile_name: Option<String>,
           remaps: Vec<RemapEntry>,
       },

       /// List of available profiles
       RemapProfiles {
           device_path: String,
           profiles: Vec<RemapProfileInfo>,
       },

       /// Profile activation confirmation
       ProfileActivated {
           device_path: String,
           profile_name: String,
       },

       /// Profile deactivation confirmation
       ProfileDeactivated {
           device_path: String,
       },
       ```

    REFERENCE: Existing Request/Response patterns (lines 72-159)
  </action>
  <verify>
    grep -n "GetActiveRemaps\|ListRemapProfiles\|ActivateRemapProfile\|DeactivateRemapProfile" razermapper/razermapper-common/src/lib.rs
    Verify Request variants exist
    grep -n "ActiveRemaps\|RemapProfiles\|ProfileActivated\|ProfileDeactivated" razermapper/razermapper-common/src/lib.rs
    Verify Response variants exist
  </verify>
  <done>
    razermapper-common has:
    - RemapProfileInfo struct
    - Request::GetActiveRemaps, ListRemapProfiles, ActivateRemapProfile, DeactivateRemapProfile
    - Response::ActiveRemaps, RemapProfiles, ProfileActivated, ProfileDeactivated
  </done>
</task>

<task type="auto">
  <name>Add IPC handler scaffolds</name>
  <files>razermapper/razermapperd/src/ipc.rs</files>
  <action>
    Add handler scaffolds to razermapper/razermapperd/src/ipc.rs:

    In the handle_request method (or where Request match arms exist), add scaffolds:
    ```rust
    // Handle GetActiveRemaps request
    Request::GetActiveRemaps { device_path } => {
        // TODO: Implement in plan 02-05
        warn!("GetActiveRemaps not yet implemented");
        Response::Error("GetActiveRemaps not yet implemented".to_string())
    }

    // Handle ListRemapProfiles request
    Request::ListRemapProfiles { device_path } => {
        // TODO: Implement in plan 02-05
        warn!("ListRemapProfiles not yet implemented");
        Response::Error("ListRemapProfiles not yet implemented".to_string())
    }

    // Handle ActivateRemapProfile request
    Request::ActivateRemapProfile { device_path, profile_name } => {
        // TODO: Implement in plan 02-05
        warn!("ActivateRemapProfile not yet implemented");
        Response::Error("ActivateRemapProfile not yet implemented".to_string())
    }

    // Handle DeactivateRemapProfile request
    Request::DeactivateRemapProfile { device_path } => {
        // TODO: Implement in plan 02-05
        warn!("DeactivateRemapProfile not yet implemented");
        Response::Error("DeactivateRemapProfile not yet implemented".to_string())
    }
    ```

    These scaffolds allow compilation while deferring implementation to plan 02-05.

    REFERENCE: Existing handler pattern in ipc.rs
  </action>
  <verify>
    grep -n "GetActiveRemaps\|ListRemapProfiles\|ActivateRemapProfile\|DeactivateRemapProfile" razermapper/razermapperd/src/ipc.rs
    Verify handler scaffolds exist
  </verify>
  <done>
    ipc.rs has handler scaffolds for all 4 profile operations with TODO comments
  </done>
</task>

</tasks>

<verification>
After all tasks complete, verify:

1. cargo check -p razermapper-common compiles
2. cargo check -p razermapperd compiles
3. All 4 Request variants exist
4. All 4 Response variants exist
5. Handler scaffolds in ipc.rs compile
</verification>

<success_criteria>
1. RemapProfileInfo struct exists with name, description, remap_count fields
2. Request enum has 4 new variants for profile operations
3. Response enum has 4 corresponding response variants
4. IPC handler scaffolds compile with TODO comments
</success_criteria>

<output>
After completion, create `.planning/phases/02-device-profiles-ipc/02-03-SUMMARY.md` with:
- IPC protocol extension summary
- Handler scaffolds for later implementation
</output>
