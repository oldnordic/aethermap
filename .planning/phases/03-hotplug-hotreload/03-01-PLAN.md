---
phase: 03-hotplug-hotreload
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [razermapper/razermapperd/src/hotplug.rs, razermapper/razermapperd/src/device.rs]
autonomous: false

must_haves:
  truths:
    - "Device hotplug monitoring detects device add/remove events"
    - "Device add events trigger device re-discovery and profile application"
    - "Device remove events trigger graceful cleanup (ungrab, removal from tracking)"
    - "Daemon continues operating after device hotplug events"
    - "Udev monitor runs in spawn_blocking to avoid blocking async runtime"
  artifacts:
    - path: "razermapper/razermapperd/src/hotplug.rs"
      provides: "Udev-based device hotplug monitoring"
      exports: ["DeviceMonitor", "DeviceEvent"]
      min_lines: 215
    - path: "razermapper/razermapperd/src/device.rs"
      provides: "Device add/remove event handlers"
      exports: ["handle_device_add", "handle_device_removal"]
      contains: "handle_device_removal"
  key_links:
    - from: "razermapper/razermapperd/src/main.rs"
      to: "razermapper/razermapperd/src/hotplug.rs"
      via: "DeviceMonitor::new() and device_monitor.recv()"
      pattern: "DeviceMonitor::new"
    - from: "razermapper/razermapperd/src/hotplug.rs"
      to: "razermapper/razermapperd/src/device.rs"
      via: "DeviceEvent::Add/Remove triggers handle_device_add/handle_device_removal"
      pattern: "handle_device_(add|removal)"
---

<objective>
Verify existing udev-based hotplug monitoring implementation

This plan validates that the DeviceMonitor in hotplug.rs correctly detects device
add/remove events via udev, and that the DeviceManager handlers properly respond
to these events. The implementation already exists; this plan focuses on
verification and documentation of the existing behavior.

Purpose: Ensure device hotplug works reliably without crashing the daemon
Output: Verified hotplug detection and handling, documentation of behavior
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-hotplug-hotreload/03-RESEARCH.md

# Existing implementation
@razermapper/razermapperd/src/hotplug.rs
@razermapper/razermapperd/src/device.rs
@razermapper/razermapperd/src/main.rs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify udev monitor implementation</name>
  <files>razermapper/razermapperd/src/hotplug.rs</files>
  <action>
  Verify the existing DeviceMonitor implementation:
  1. Confirm udev::MonitorBuilder monitors "input" subsystem
  2. Confirm spawn_blocking is used for the blocking udev socket iteration
  3. Confirm DeviceEvent enum has Add and Remove variants with device_id
  4. Confirm device_map HashMap tracks devnode -> device_id for remove events
  5. Confirm extract_device_id() uses evdev::Device::input_id() for VID/PID

  Do NOT modify the implementation - only verify and document findings.
  </action>
  <verify>
  Read hotplug.rs and confirm:
  - Line 120-122: MonitorBuilder with match_subsystem("input")
  - Line 68-73: tokio::task::spawn_blocking for udev monitor
  - Line 26-42: DeviceEvent enum with Add/Remove variants
  - Line 127: device_map HashMap for tracking
  </verify>
  <done>
  Udev monitor implementation verified with all required components present
  </done>
</task>

<task type="auto">
  <name>Task 2: Verify device add handler</name>
  <files>razermapper/razermapperd/src/device.rs</files>
  <action>
  Verify the handle_device_add() method in device.rs:
  1. Confirm method re-opens device with evdev::Device::open()
  2. Confirm method extracts VID/PID and formats device_id
  3. Confirm method checks config_manager for device profiles
  4. Confirm method calls grab_device() if profiles exist
  5. Confirm method adds device to devices HashMap

  Do NOT modify - only verify and document.
  </action>
  <verify>
  Read device.rs handle_device_add() (lines 277-337) and confirm:
  - Line 287-294: Device re-open and VID/PID extraction
  - Line 297: device_id formatting with format_device_id()
  - Line 313-325: Profile check via config_manager.list_device_profiles()
  - Line 329: grab_device() call if profiles exist
  </verify>
  <done>
  Device add handler verified to re-discover, check profiles, and auto-grab
  </done>
</task>

<task type="auto">
  <name>Task 3: Verify device removal handler</name>
  <files>razermapper/razermapperd/src/device.rs</files>
  <action>
  Verify the handle_device_removal() method in device.rs:
  1. Confirm method calls ungrab_device() with error handling (best-effort)
  2. Confirm method removes device from devices HashMap
  3. Confirm errors are logged but don't cause failure (best-effort cleanup)
  4. Confirm Drop trait on GrabbedDevice also handles ungrab on panic

  Do NOT modify - only verify and document.
  </action>
  <verify>
  Read device.rs handle_device_removal() (lines 244-259) and confirm:
  - Line 248-250: ungrab_device() wrapped in if let Err()
  - Line 253-255: Removal from devices HashMap
  - Line 32-38: Drop trait implementation for GrabbedDevice
  </verify>
  <done>
  Device removal handler verified to clean up gracefully with best-effort ungrab
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
  Verification complete for udev hotplug monitoring implementation:
  - DeviceMonitor spawns udev monitor in blocking task
  - DeviceEvent::Add/Remove flow to device manager handlers
  - handle_device_add() re-discovers and auto-grabs devices with profiles
  - handle_device_removal() performs best-effort cleanup
  - Drop trait ensures device release on panic

  Key finding: Implementation uses udev (not inotify as ROADMAP states)
  </what-built>
  <how-to-verify>
  1. Review the verification findings above
  2. Check if hotplug behavior is documented for users
  3. Decide if ROADMAP.md should be updated (inotify -> udev terminology)
  4. Type "approved" to continue or describe issues
  </how-to-verify>
  <resume-signal>Type "approved" to continue, or describe any concerns</resume-signal>
</task>

</tasks>

<verification>
- [ ] DeviceMonitor implementation verified
- [ ] Device add handler verified
- [ ] Device removal handler verified
- [ ] User approval of verification findings
</verification>

<success_criteria>
Existing udev-based hotplug monitoring implementation verified and documented.
ROADMAP.md terminology noted for update (inotify -> udev).
</success_criteria>

<output>
After completion, create `.planning/phases/03-hotplug-hotreload/03-01-SUMMARY.md`
</output>
