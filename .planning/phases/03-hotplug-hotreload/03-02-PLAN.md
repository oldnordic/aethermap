---
phase: 03-hotplug-hotreload
plan: 02
type: execute
wave: 1
depends_on: []
files_modified: [razermapper/razermapperd/src/config.rs, razermapper/razermapperd/src/main.rs]
autonomous: false

must_haves:
  truths:
    - "SIGHUP signal triggers configuration reload without daemon restart"
    - "Configuration reload validates all keys before applying changes"
    - "Invalid configurations are rejected with clear error messages"
    - "Valid configurations are applied atomically (all-or-nothing)"
    - "Device profiles and global remaps both reload on SIGHUP"
  artifacts:
    - path: "razermapper/razermapperd/src/config.rs"
      provides: "Atomic configuration hot-reload methods"
      exports: ["reload_device_profiles", "reload_remaps"]
      contains: "reload_device_profiles"
    - path: "razermapper/razermapperd/src/main.rs"
      provides: "SIGHUP signal handler"
      contains: "sig_hup"
  key_links:
    - from: "razermapper/razermapperd/src/main.rs"
      to: "razermapper/razermapperd/src/config.rs"
      via: "SIGHUP handler calls reload_device_profiles() and reload_remaps()"
      pattern: "reload_(device_profiles|remaps)"
    - from: "razermapper/razermapperd/src/config.rs"
      to: "razermapper/razermapperd/src/remap_engine.rs"
      via: "reload_remaps() applies new config to RemapEngine"
      pattern: "load_config"
---

<objective>
Verify existing SIGHUP-based configuration hot-reload implementation

This plan validates that configuration reload works without daemon restart
via SIGHUP signal. The implementation already exists with atomic swap
semantics and eager validation. This plan focuses on verification and
documentation of the existing behavior.

Purpose: Ensure configuration reload works safely without service interruption
Output: Verified hot-reload behavior, documentation of atomic swap pattern
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-hotplug-hotreload/03-RESEARCH.md

# Existing implementation
@razermapper/razermapperd/src/config.rs
@razermapper/razermapperd/src/main.rs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify SIGHUP signal handler</name>
  <files>razermapper/razermapperd/src/main.rs</files>
  <action>
  Verify the SIGHUP signal handler in main.rs:
  1. Confirm tokio::signal::unix::SignalKind::hangup() is used
  2. Confirm sig_hup.recv() branch in tokio::select!
  3. Confirm handler calls reload_device_profiles() and reload_remaps()
  4. Confirm errors are logged but daemon continues running
  5. Confirm nested tokio::select! allows continued event handling after SIGHUP

  Do NOT modify - only verify and document.
  </action>
  <verify>
  Read main.rs and confirm:
  - Line 230: sig_hup signal initialization
  - Line 234-250: SIGHUP recv branch with reload calls
  - Line 251-288: Nested tokio::select! for continued operation
  - Line 237-249: Error logging with daemon continuation
  </verify>
  <done>
  SIGHUP handler verified to trigger reload without daemon restart
  </done>
</task>

<task type="auto">
  <name>Task 2: Verify device profile reload</name>
  <files>razermapper/razermapperd/src/config.rs</files>
  <action>
  Verify the reload_device_profiles() method in config.rs:
  1. Confirm method reads YAML file before acquiring write lock
  2. Confirm method validates all profiles with KeyParser before swap
  3. Confirm method performs atomic swap via *config_guard = all_profiles
  4. Confirm method returns error on validation failure (no partial apply)
  5. Confirm method uses Arc<RwLock<>> for thread-safe access

  Do NOT modify - only verify and document.
  </action>
  <verify>
  Read config.rs reload_device_profiles() (lines 717-795) and confirm:
  - Line 724-736: File read and YAML parse before lock
  - Line 744-780: Validation loop with KeyParser
  - Line 785-786: Atomic swap inside write lock
  - Line 770-775: Error return on invalid profile
  </verify>
  <done>
  Device profile reload verified to use validate-then-swap pattern
  </done>
</task>

<task type="auto">
  <name>Task 3: Verify global remap reload</name>
  <files>razermapper/razermapperd/src/config.rs</files>
  <action>
  Verify the reload_remaps() method in config.rs:
  1. Confirm method calls load_remaps() for validation
  2. Confirm method applies validated config to RemapEngine
  3. Confirm method returns error if RemapEngine rejects config
  4. Confirm existing RemapEngine state unchanged on failure

  Do NOT modify - only verify and document.
  </action>
  <verify>
  Read config.rs reload_remaps() (lines 826-860) and confirm:
  - Line 836: load_remaps() call for validation
  - Line 845-852: RemapEngine.load_config() application
  - Line 848-852: Error return with RemapConfigError on failure
  </verify>
  <done>
  Global remap reload verified to validate before applying to RemapEngine
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
  Verification complete for SIGHUP-based configuration hot-reload:
  - tokio::signal::unix handles SIGHUP in async runtime
  - reload_device_profiles() uses validate-then-swap pattern
  - reload_remaps() validates before applying to RemapEngine
  - Atomic swap via Arc<RwLock<>> ensures all-or-nothing updates
  - Errors logged but daemon continues with old configuration

  Pattern: Read file -> Parse -> Validate -> (only then) Acquire lock -> Swap
  </what-built>
  <how-to-verify>
  1. Review the verification findings above
  2. Confirm atomic swap pattern is documented
  3. Type "approved" to continue or describe issues
  </how-to-verify>
  <resume-signal>Type "approved" to continue, or describe any concerns</resume-signal>
</task>

</tasks>

<verification>
- [ ] SIGHUP handler verified
- [ ] Device profile reload verified
- [ ] Global remap reload verified
- [ ] User approval of verification findings
</verification>

<success_criteria>
Existing SIGHUP-based hot-reload implementation verified with atomic swap pattern documented.
</success_criteria>

<output>
After completion, create `.planning/phases/03-hotplug-hotreload/03-02-SUMMARY.md`
</output>
