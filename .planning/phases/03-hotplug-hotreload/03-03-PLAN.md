---
phase: 03-hotplug-hotreload
plan: 03
type: execute
wave: 2
depends_on: [03-01, 03-02]
files_modified: [aethermap/aethermapd/src/hotplug.rs, aethermap/aethermapd/src/device.rs, aethermap/aethermapd/src/config.rs]
autonomous: true

must_haves:
  truths:
    - "Integration tests verify device add/remove scenarios"
    - "Integration tests verify SIGHUP configuration reload"
    - "Tests use mock udev events or virtual devices"
    - "All tests pass cleanly"
  artifacts:
    - path: "aethermap/aethermapd/tests/hotplug_test.rs"
      provides: "Integration tests for hotplug scenarios"
      min_lines: 50
      covered_by: "Task 1"
    - path: "aethermap/aethermapd/tests/config_reload_test.rs"
      provides: "Integration tests for config reload validation"
      min_lines: 50
      covered_by: "Task 2"
  key_links:
    - from: "aethermap/aethermapd/tests/hotplug_test.rs"
      to: "aethermap/aethermapd/src/hotplug.rs"
      via: "Use hotplug::DeviceEvent and hotplug::DeviceMonitor in tests"
      pattern: "hotplug::(DeviceEvent|DeviceMonitor)"
    - from: "aethermap/aethermapd/tests/config_reload_test.rs"
      to: "aethermap/aethermapd/src/config.rs"
      via: "Test reload_device_profiles() and reload_remaps() methods"
      pattern: "reload_(device_profiles|remaps)"
---

<objective>
Add integration tests for hotplug and hot-reload scenarios

This plan creates test coverage for the existing hotplug and hot-reload
functionality. Tests verify that device add/remove events are handled
correctly and that SIGHUP triggers configuration reload safely.

Purpose: Ensure hotplug and hot-reload behavior is tested and verified
Output: Integration tests covering hotplug and reload scenarios
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-hotplug-hotreload/03-RESEARCH.md

# Existing implementation (verified in 03-01 and 03-02)
@aethermap/aethermapd/src/hotplug.rs
@aethermap/aethermapd/src/device.rs
@aethermap/aethermapd/src/config.rs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create tests for device event handling</name>
  <files>aethermap/aethermapd/tests/hotplug_test.rs</files>
  <action>
  Create test file aethermap/aethermapd/tests/hotplug_test.rs:

  1. Add test module for DeviceEvent verification
  2. Write test_device_event_add() - verifies Add event structure
  3. Write test_device_event_remove() - verifies Remove event structure
  4. Write test_format_device_id() - verifies VID/PID formatting
  5. Use hotplug::DeviceEvent type to verify test wiring to implementation
  6. Run tests with `cargo test --package aethermapd --test hotplug_test`
  7. Verify all tests pass (existing code has correct behavior)

  Focus on testing the public API of hotplug module.
  </action>
  <verify>
  Run `cargo test --package aethermapd --test hotplug_test` and verify all tests pass
  </verify>
  <done>
  Device event tests pass, verifying event structure and device ID formatting
  </done>
</task>

<task type="auto">
  <name>Task 2: Create tests for config reload validation</name>
  <files>aethermap/aethermapd/tests/config_reload_test.rs</files>
  <action>
  Create test file aethermap/aethermapd/tests/config_reload_test.rs:

  1. Add test for valid config reload (atomic swap)
  2. Add test for invalid config rejection (bad key names)
  3. Use tempfile for temporary YAML files
  4. Test both reload_remaps() and reload_device_profiles()
  5. Run `cargo test --package aethermapd --test config_reload_test`
  6. Verify tests pass

  Focus on validation behavior - config rejected on invalid keys.
  </action>
  <verify>
  Run `cargo test --package aethermapd --test config_reload_test` and verify all tests pass
  </verify>
  <done>
  Config reload tests pass, verifying validate-then-swap pattern
  </done>
</task>

<task type="auto">
  <name>Task 3: Clean up and document test patterns</name>
  <files>aethermap/aethermapd/tests/hotplug_test.rs, aethermap/aethermapd/tests/config_reload_test.rs</files>
  <action>
  Review and refactor test code:
  1. Extract common test helpers if duplicated
  2. Add documentation comments explaining test scenarios
  3. Ensure tests are isolated (no shared state between tests)
  4. Run full test suite: `cargo test --package aethermapd`
  5. Verify all tests pass including existing unit tests

  Focus on test clarity and maintainability.
  </action>
  <verify>
  Run `cargo test --package aethermapd` and verify all tests pass
  </verify>
  <done>
  Test suite clean, documented, and passing
  </done>
</task>

</tasks>

<verification>
- [ ] hotplug_test.rs created with device event tests
- [ ] config_reload_test.rs created with reload validation tests
- [ ] All new tests pass
- [ ] Full test suite passes
</verification>

<success_criteria>
Integration tests for hotplug and hot-reload scenarios pass cleanly.
Test coverage documents expected behavior for future maintenance.
</success_criteria>

<output>
After completion, create `.planning/phases/03-hotplug-hotreload/03-03-SUMMARY.md`
</output>
