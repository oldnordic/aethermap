---
phase: 03-hotplug-hotreload
plan: 03
type: tdd
wave: 2
depends_on: [03-01, 03-02]
files_modified: [razermapper/razermapperd/src/hotplug.rs, razermapper/razermapperd/src/device.rs, razermapper/razermapperd/src/config.rs]
autonomous: true

must_haves:
  truths:
    - "Integration tests verify device add/remove scenarios"
    - "Integration tests verify SIGHUP configuration reload"
    - "Tests use mock udev events or virtual devices"
    - "All tests pass cleanly"
  artifacts:
    - path: "razermapper/razermapperd/src/hotplug.rs"
      provides: "Unit tests for DeviceMonitor"
      contains: "#[cfg(test)]"
    - path: "razermapper/razermapperd/tests/hotplug_test.rs"
      provides: "Integration tests for hotplug scenarios"
      min_lines: 50
  key_links:
    - from: "razermapper/razermapperd/tests/hotplug_test.rs"
      to: "razermapper/razermapperd/src/hotplug.rs"
      via: "Use DeviceMonitor and DeviceEvent types in tests"
      pattern: "hotplug::"
---

<objective>
Add integration tests for hotplug and hot-reload scenarios

This plan creates test coverage for the existing hotplug and hot-reload
functionality. Tests verify that device add/remove events are handled
correctly and that SIGHUP triggers configuration reload safely.

Purpose: Ensure hotplug and hot-reload behavior is tested and verified
Output: Integration tests covering hotplug and reload scenarios
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-hotplug-hotreload/03-RESEARCH.md

# Existing implementation (verified in 03-01 and 03-02)
@razermapper/razermapperd/src/hotplug.rs
@razermapper/razermapperd/src/device.rs
@razermapper/razermapperd/src/config.rs
</context>

<feature>
  <name>Hotplug and hot-reload integration tests</name>
  <files>razermapper/razermapperd/tests/hotplug_test.rs</files>
  <behavior>
    Test scenarios:
    1. DeviceEvent::Add is received and processed correctly
    2. DeviceEvent::Remove is received and processed correctly
    3. Rapid add/remove sequences are handled without crashes
    4. Configuration reload validates before applying
    5. Invalid configuration is rejected without crashing

    Input: Simulated DeviceEvent or SIGHUP signal
    Expected Output: Proper handler execution, no crashes, errors logged
  </behavior>
  <implementation>
    Create tests/hotplug_test.rs with:
    1. Unit tests for DeviceEvent creation and format_device_id()
    2. Mock-based tests for device add/remove handling
    3. Integration-style tests for config reload validation
    4. Use tokio::test for async test cases
    5. Verify error handling paths (invalid config, missing device)
  </implementation>
</feature>

<tasks>

<task type="auto">
  <name>RED: Create failing test for device event handling</name>
  <files>razermapper/razermapperd/tests/hotplug_test.rs</files>
  <action>
  Create test file razermapper/razermapperd/tests/hotplug_test.rs:

  1. Add test module for DeviceEvent verification
  2. Write test_device_event_add() - verifies Add event structure
  3. Write test_device_event_remove() - verifies Remove event structure
  4. Write test_format_device_id() - verifies VID/PID formatting
  5. Run tests with `cargo test --package razermapperd --test hotplug_test`
  6. Tests should compile and pass (existing code has correct behavior)

  Focus on testing the public API of hotplug module.
  </action>
  <verify>
  Run `cargo test --package razermapperd --test hotplug_test` and verify all tests pass
  </verify>
  <done>
  Device event tests pass, verifying event structure and device ID formatting
  </done>
</task>

<task type="auto">
  <name>GREEN: Add tests for config reload validation</name>
  <files>razermapper/razermapperd/tests/config_reload_test.rs</files>
  <action>
  Create test file razermapper/razermapperd/tests/config_reload_test.rs:

  1. Add test for valid config reload (atomic swap)
  2. Add test for invalid config rejection (bad key names)
  3. Use tempfile for temporary YAML files
  4. Test both reload_remaps() and reload_device_profiles()
  5. Run `cargo test --package razermapperd --test config_reload_test`
  6. Verify tests pass

  Focus on validation behavior - config rejected on invalid keys.
  </action>
  <verify>
  Run `cargo test --package razermapperd --test config_reload_test` and verify all tests pass
  </verify>
  <done>
  Config reload tests pass, verifying validate-then-swap pattern
  </done>
</task>

<task type="auto">
  <name>REFACTOR: Clean up and document test patterns</name>
  <files>razermapper/razermapperd/tests/hotplug_test.rs, razermapper/razermapperd/tests/config_reload_test.rs</files>
  <action>
  Review and refactor test code:
  1. Extract common test helpers if duplicated
  2. Add documentation comments explaining test scenarios
  3. Ensure tests are isolated (no shared state between tests)
  4. Run full test suite: `cargo test --package razermapperd`
  5. Verify all tests pass including existing unit tests

  Focus on test clarity and maintainability.
  </action>
  <verify>
  Run `cargo test --package razermapperd` and verify all tests pass
  </verify>
  <done>
  Test suite clean, documented, and passing
  </done>
</task>

</tasks>

<verification>
- [ ] hotplug_test.rs created with device event tests
- [ ] config_reload_test.rs created with reload validation tests
- [ ] All new tests pass
- [ ] Full test suite passes
</verification>

<success_criteria>
Integration tests for hotplug and hot-reload scenarios pass cleanly.
Test coverage documents expected behavior for future maintenance.
</success_criteria>

<output>
After completion, create `.planning/phases/03-hotplug-hotreload/03-03-SUMMARY.md`
</output>
