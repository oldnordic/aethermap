---
phase: 17-hotkey-and-auto-switch-persistence
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - razermapper/razermapperd/src/config.rs
autonomous: true

must_haves:
  truths:
    - "ConfigManager can add a hotkey binding to a device's configuration"
    - "ConfigManager can remove a hotkey binding from a device's configuration"
    - "ConfigManager can list all hotkey bindings for a device"
    - "Duplicate hotkey bindings (same key + modifiers) are rejected"
    - "All methods serialize changes to device_profiles.yaml"
  artifacts:
    - path: "razermapper/razermapperd/src/config.rs"
      provides: "ConfigManager hotkey binding CRUD operations"
      exports: ["add_hotkey_binding", "remove_hotkey_binding", "get_hotkey_bindings"]
      covered_by: "Task 1, Task 2, Task 3"
  key_links:
    - from: "ConfigManager::add_hotkey_binding"
      to: "device_profiles.yaml"
      via: "tokio::fs::write"
      pattern: "fs::write.*device_profiles_path"
    - from: "ConfigManager::remove_hotkey_binding"
      to: "ExtendedDeviceRemapConfig.hotkey_bindings"
      via: "Vec::retain"
      pattern: "hotkey_bindings.retain"
---

<objective>
Implement ConfigManager methods for hotkey binding persistence, enabling add/remove/list operations that serialize to device_profiles.yaml following the existing set_analog_calibration pattern.

Purpose: Hotkey bindings need to persist across daemon restarts. These ConfigManager methods provide the foundation for IPC handlers to read/write hotkey configuration.

Output: Three new methods in ConfigManager impl block: `add_hotkey_binding()`, `remove_hotkey_binding()`, `get_hotkey_bindings()`.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/17-hotkey-and-auto-switch-persistence/17-RESEARCH.md
@.planning/phases/13-wayland-portal-integration/13-04-SUMMARY.md

@razermapper/razermapperd/src/config.rs (lines 2010-2060 for set_analog_calibration pattern)
@razermapper/razermapperd/src/config.rs (lines 515-555 for ExtendedDeviceRemapConfig structure)
</context>

<tasks>

<task type="auto">
  <name>Add ConfigManager::add_hotkey_binding method</name>
  <files>razermapper/razermapperd/src/config.rs</files>
  <action>
    Add `add_hotkey_binding()` method to ConfigManager impl block, after the `set_analog_calibration()` method (around line 2065).

    Signature:
    ```rust
    pub async fn add_hotkey_binding(
        &self,
        device_id: &str,
        binding: HotkeyBinding,
    ) -> Result<(), RemapConfigError>
    ```

    Implementation following set_analog_calibration pattern:
    1. Load RemapDevicesConfig from device_profiles_path
    2. Get or create device entry with full ExtendedDeviceRemapConfig default (including hotkey_bindings: Vec::new())
    3. Check for duplicate: existing.key == binding.key AND existing.modifiers == binding.modifiers
    4. If duplicate found, return RemapConfigError::Validation with message "Hotkey with this key and modifiers already exists"
    5. Push binding to device_entry.hotkey_bindings
    6. Serialize to YAML with serde_yaml::to_string
    7. Write to device_profiles_path with tokio::fs::write
    8. Log info message: "Added hotkey binding for device {}"

    Use exact error handling pattern from set_analog_calibration (lines 2048-2058).

    DO NOT block tokio executor - use async fs::write only.
  </action>
  <verify>cargo check --manifest-path razermapper/razermapperd/Cargo.toml</verify>
  <done>add_hotkey_binding compiles and follows set_analog_calibration pattern with duplicate detection</done>
</task>

<task type="auto">
  <name>Add ConfigManager::remove_hotkey_binding method</name>
  <files>razermapper/razermapperd/src/config.rs</files>
  <action>
    Add `remove_hotkey_binding()` method to ConfigManager impl block, after add_hotkey_binding.

    Signature:
    ```rust
    pub async fn remove_hotkey_binding(
        &self,
        device_id: &str,
        key: &str,
        modifiers: &[String],
    ) -> Result<(), RemapConfigError>
    ```

    Implementation:
    1. Load RemapDevicesConfig from device_profiles_path (return error if file doesn't exist)
    2. Get device entry, return error if device_id not found
    3. Get original length of hotkey_bindings
    4. Use retain() to keep bindings that DON'T match (binding.key != key OR binding.modifiers != modifiers)
    5. If length unchanged (no binding removed), return RemapConfigError::Validation with "Hotkey not found"
    6. Serialize to YAML and write to device_profiles_path
    7. Log info message: "Removed hotkey binding for device {}"

    Use same error handling pattern as add_hotkey_binding.

    DO NOT use Vec::remove with index - use retain for functional iteration.
  </action>
  <verify>cargo check --manifest-path razermapper/razermapperd/Cargo.toml</verify>
  <done>remove_hotkey_binding compiles and removes matching bindings from YAML config</done>
</task>

<task type="auto">
  <name>Add ConfigManager::get_hotkey_bindings method</name>
  <files>razermapper/razermapperd/src/config.rs</files>
  <action>
    Add `get_hotkey_bindings()` method to ConfigManager impl block, after remove_hotkey_binding.

    Signature:
    ```rust
    pub async fn get_hotkey_bindings(
        &self,
        device_id: &str,
    ) -> Result<Vec<HotkeyBinding>, RemapConfigError>
    ```

    Implementation:
    1. Read device_profiles_path, return empty Vec if file doesn't exist (not an error)
    2. Parse YAML to RemapDevicesConfig
    3. Get device entry, return empty Vec if device_id not found (not an error)
    4. Clone and return device_entry.hotkey_bindings

    Use graceful degradation: missing file or device returns empty Vec, not error.
    This matches ListMacros pattern where absence means "no bindings configured".

    DO NOT return error for missing device - treat as empty bindings list.
  </action>
  <verify>cargo check --manifest-path razermapper/razermapperd/Cargo.toml</verify>
  <done>get_hotkey_bindings compiles and returns Vec<HotkeyBinding> for device</done>
</task>

</tasks>

<verification>
- [ ] add_hotkey_binding follows set_analog_calibration pattern exactly
- [ ] add_hotkey_binding rejects duplicate bindings with Validation error
- [ ] remove_hotkey_binding uses retain() to filter bindings
- [ ] remove_hotkey_binding returns error when binding not found
- [ ] get_hotkey_bindings returns empty Vec for missing file/device
- [ ] All methods use tokio::fs::write for async file I/O
- [ ] All methods log with info! for successful operations
- [ ] cargo check passes with no errors
</verification>

<success_criteria>
1. ConfigManager has add_hotkey_binding, remove_hotkey_binding, get_hotkey_bindings methods
2. All methods compile and follow existing config.rs patterns
3. Methods serialize to device_profiles.yaml correctly
4. Duplicate detection works in add_hotkey_binding
</success_criteria>

<output>
After completion, create `.planning/phases/17-hotkey-and-auto-switch-persistence/17-01-SUMMARY.md`
</output>
