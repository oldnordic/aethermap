---
phase: 17-hotkey-and-auto-switch-persistence
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - razermapper/razermapperd/src/config.rs
autonomous: true

must_haves:
  truths:
    - "ConfigManager can set global auto-switch rules"
    - "ConfigManager can get global auto-switch rules"
    - "Auto-switch rules persist to config.yaml (not device_profiles.yaml)"
    - "Methods update in-memory config and serialize to file"
  artifacts:
    - path: "razermapper/razermapperd/src/config.rs"
      provides: "ConfigManager auto-switch rule persistence"
      exports: ["set_auto_switch_rules", "get_auto_switch_rules"]
      covered_by: "Task 1, Task 2"
  key_links:
    - from: "ConfigManager::set_auto_switch_rules"
      to: "config.yaml"
      via: "tokio::fs::write"
      pattern: "fs::write.*config_path"
    - from: "ConfigManager::get_auto_switch_rules"
      to: "DaemonConfig.auto_switch_rules"
      via: "field access"
      pattern: "self.config.auto_switch_rules"
---

<objective>
Implement ConfigManager methods for global auto-switch rule persistence, enabling set/get operations that serialize to config.yaml (the main daemon config file, distinct from device_profiles.yaml).

Purpose: Auto-switch rules are global configuration (not per-device) and stored in config.yaml. These methods provide the persistence layer for the GUI to manage auto-profile switching rules.

Output: Two new methods in ConfigManager impl block: `set_auto_switch_rules()`, `get_auto_switch_rules()`.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/17-hotkey-and-auto-switch-persistence/17-RESEARCH.md
@.planning/phases/13-wayland-portal-integration/13-04-SUMMARY.md

@razermapper/razermapperd/src/config.rs (lines 764-776 for DaemonConfig.auto_switch_rules)
@razermapper/razermapperd/src/config.rs (lines 974-991 for save_config pattern)
</context>

<tasks>

<task type="auto">
  <name>Add ConfigManager::set_auto_switch_rules method</name>
  <files>razermapper/razermapperd/src/config.rs</files>
  <action>
    Add `set_auto_switch_rules()` method to ConfigManager impl block, near other config-level methods (around line 995, after save_config).

    Signature:
    ```rust
    pub async fn set_auto_switch_rules(
        &self,
        rules: Vec<AutoSwitchRule>,
    ) -> Result<(), RemapConfigError>
    ```

    Implementation:
    1. Acquire write lock on self.config
    2. Update in-memory: config.write().auto_switch_rules = rules.clone()
    3. Drop lock, serialize full DaemonConfig to YAML using serde_yaml::to_string(&self.config)
    4. Handle serialization error with RemapConfigError::WriteError
    5. Write to self.config_path with tokio::fs::write
    6. Log info message: "Saved {} auto-switch rules" with rules.len()

    Important: Read self.config with read lock first to clone, then serialize.
    Or use read lock for the entire operation since we're reading then writing (double-checked locking pattern acceptable here).

    DO NOT use device_profiles_path - auto-switch rules go in config.yaml.
  </action>
  <verify>cargo check --manifest-path razermapper/razermapperd/Cargo.toml</verify>
  <done>set_auto_switch_rules compiles and writes to config.yaml</done>
</task>

<task type="auto">
  <name>Add ConfigManager::get_auto_switch_rules method</name>
  <files>razermapper/razermapperd/src/config.rs</files>
  <action>
    Add `get_auto_switch_rules()` method to ConfigManager impl block, after set_auto_switch_rules.

    Signature:
    ```rust
    pub async fn get_auto_switch_rules(&self) -> Vec<AutoSwitchRule>
    ```

    Implementation:
    1. Acquire read lock on self.config
    2. Clone and return config.auto_switch_rules

    Simple getter - no file I/O needed (reads from in-memory config).

    DO NOT return Result - just return Vec (empty if none configured).
  </action>
  <verify>cargo check --manifest-path razermapper/razermapperd/Cargo.toml</verify>
  <done>get_auto_switch_rules compiles and returns Vec<AutoSwitchRule></done>
</task>

</tasks>

<verification>
- [ ] set_auto_switch_rules writes to config.yaml (not device_profiles.yaml)
- [ ] set_auto_switch_rules updates in-memory config
- [ ] get_auto_switch_rules reads from in-memory config
- [ ] Both methods compile with no errors
- [ ] Log messages present for successful operations
</verification>

<success_criteria>
1. ConfigManager has set_auto_switch_rules and get_auto_switch_rules methods
2. Methods read/write to config.yaml correctly
3. In-memory config stays synchronized with file
</success_criteria>

<output>
After completion, create `.planning/phases/17-hotkey-and-auto-switch-persistence/17-02-SUMMARY.md`
</output>
