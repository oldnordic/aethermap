---
phase: 16-calibration-gui
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - razermapper/razermapper-gui/src/widgets/analog_visualizer.rs
  - razermapper/razermapper-gui/src/widgets/mod.rs
  - razermapper/razermapper-gui/Cargo.toml
autonomous: true

must_haves:
  truths:
    - "AnalogVisualizer widget compiles and implements canvas::Program trait"
    - "Widget draws outer circle, deadzone shape, axes crosshairs, and center point"
    - "Widget accepts stick position, deadzone, deadzone shape, and range parameters"
    - "Widget is exported from widgets module for use in gui.rs"
  artifacts:
    - path: "razermapper/razermapper-gui/src/widgets/analog_visualizer.rs"
      provides: "Canvas-based stick position visualization widget"
      min_lines: 80
      exports: ["AnalogVisualizer"]
      covered_by: "Task 2"
    - path: "razermapper/razermapper-gui/src/widgets/mod.rs"
      provides: "Module declaration for widgets"
      contains: "pub mod analog_visualizer"
      covered_by: "Task 1"
  key_links:
    - from: "razermapper/razermapper-gui/src/widgets/analog_visualizer.rs"
      to: "iced::widget::canvas"
      via: "use iced::widget::canvas::{Program, Frame, Path, Geometry}"
      pattern: "canvas::Program<Message>"
---

<objective>
Create Canvas-based stick position visualization widget foundation

Purpose: The AnalogVisualizer widget provides the base infrastructure for real-time analog stick display. It implements Iced's canvas::Program trait to draw deadzone shapes (circle/square), axes, and the stick position dot. This is the visual foundation that live input data will populate in later plans.

Output: New widgets module with AnalogVisualizer that renders a static stick visualization (no real-time data yet).
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/16-calibration-gui/16-CONTEXT.md
@.planning/phases/16-calibration-gui/16-RESEARCH.md
@razermapper/razermapper-gui/src/gui.rs
@razermapper/razermapper-common/src/lib.rs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create widgets module with AnalogVisualizer struct</name>
  <files>razermapper/razermapper-gui/src/widgets/mod.rs</files>
  <action>
Create new widgets module file at razermapper/razermapper-gui/src/widgets/mod.rs with:
- pub mod analog_visualizer declaration

The mod.rs file should be minimal:
```rust
//! Custom widgets for razermapper-gui
//!
//! Provides reusable UI components beyond standard Iced widgets.

pub mod analog_visualizer;

pub use analog_visualizer::AnalogVisualizer;
```

Do NOT add any widgets module declaration to lib.rs yet - that's in a later plan.
  </action>
  <verify>cat razermapper/razermapper-gui/src/widgets/mod.rs contains "pub mod analog_visualizer"</verify>
  <done>widgets/mod.rs exists and declares analog_visualizer module</done>
</task>

<task type="auto">
  <name>Task 2: Implement AnalogVisualizer with canvas::Program trait</name>
  <files>razermapper/razermapper-gui/src/widgets/analog_visualizer.rs</files>
  <action>
Create razermapper/razermapper-gui/src/widgets/analog_visualizer.rs with:

1. Import canvas types:
   - use iced::widget::canvas::{self, Program, Frame, Path, Geometry};
   - use iced::{Color, Point, Rectangle, Size};
   - use razermapper_common::DeadzoneShape;

2. Define AnalogVisualizer struct with fields:
   - pub stick_x: f32 (current position -1.0 to 1.0)
   - pub stick_y: f32 (current position -1.0 to 1.0)
   - pub deadzone: f32 (0.0 to 1.0)
   - pub deadzone_shape: DeadzoneShape
   - pub range_min: i32
   - pub range_max: i32

3. Implement canvas::Program<Message> for AnalogVisualizer with:
   - type State = ()
   - update() is empty (no interaction)
   - draw() method that:
     a. Creates Frame from bounds.size()
     b. Calculates center and outer_radius from bounds
     c. Draws outer circle (light gray fill, darker stroke)
     d. Draws deadzone:
        - Circular: Path::circle() with green fill
        - Square: Path::rectangle() with green fill
     e. Draws crosshairs (horizontal/vertical lines)
     f. Draws stick position dot (red circle with white stroke)
        - IMPORTANT: Invert Y when converting to screen coords (screen_y = center.y - stick_y * radius)

4. Use the drawing API from RESEARCH.md Pattern 1:
   - frame.fill(&path, color) for filled shapes
   - frame.stroke(&path, color, width) for outlines

Note: This is static rendering only. Real-time updates come in Plan 04-05.
  </action>
  <verify>grep -q "impl.*canvas::Program.*for AnalogVisualizer" razermapper/razermapper-gui/src/widgets/analog_visualizer.rs</verify>
  <done>AnalogVisualizer implements canvas::Program with draw() method rendering all visual elements</done>
</task>

</tasks>

<verification>
After completion, verify:
1. Cargo check passes: cargo check --manifest-path razermapper/razermapper-gui/Cargo.toml
2. analog_visualizer.rs has at least 80 lines
3. Widget is re-exported from widgets module
</verification>

<success_criteria>
- AnalogVisualizer struct exists with all required fields
- canvas::Program trait is implemented
- draw() method renders outer circle, deadzone (circle/square), crosshairs, and stick dot
- Y-axis inversion is applied for screen coordinates
- Code compiles without errors
</success_criteria>

<output>
After completion, create `.planning/phases/16-calibration-gui/16-01-SUMMARY.md`
</output>
