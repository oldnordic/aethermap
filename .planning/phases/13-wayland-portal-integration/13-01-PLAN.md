---
phase: 13-wayland-portal-integration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - razermapper/razermapper-gui/Cargo.toml
autonomous: true
user_setup: []

must_haves:
  truths:
    - "ashpd dependency is available in GUI crate for Wayland portal integration"
    - "ashpd is configured with wayland feature only (minimal dependencies)"
    - "cargo check passes for razermapper-gui after adding dependency"
  artifacts:
    - path: "razermapper/razermapper-gui/Cargo.toml"
      provides: "ashpd dependency with wayland feature"
      contains: "ashpd = { version = \"0.8\", default-features = false, features = [\"wayland\"] }"
      covered_by: "Task 1"
      min_lines: 1
  key_links:
    - from: "razermapper/razermapper-gui/src/lib.rs"
      to: "ashpd crate"
      via: "extern crate ashpd"
      pattern: "use ashpd::"
---

<objective>
Add ashpd dependency to GUI for Wayland xdg-desktop-portal integration

Purpose: Provides the foundation for Wayland portal integration. The ashpd crate
(Async Portal Desktop) is the standard Rust bindings for xdg-desktop-portal,
enabling window focus detection without compositor-specific workarounds.

Output: ashpd dependency in razermapper-gui/Cargo.toml with wayland feature
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@razermapper/razermapper-gui/Cargo.toml
@.planning/phases/13-wayland-portal-integration/13-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Add ashpd dependency to razermapper-gui Cargo.toml</name>
  <files>razermapper/razermapper-gui/Cargo.toml</files>
  <action>
Add ashpd dependency to razermapper/razermapper-gui/Cargo.toml:

1. Add to [dependencies] section:
   ```toml
   ashpd = { version = "0.8", default-features = false, features = ["wayland"] }
   ```

Key design decisions:
- Version 0.8 is latest stable with good Wayland support
- default-features = false minimizes dependency bloat
- wayland feature only (no need for gtk4 or other portal backends)
- GUI runs as normal user (required for portal access, daemon runs as root)
  </action>
  <verify>
grep -n "ashpd" razermapper/razermapper-gui/Cargo.toml
cargo check --manifest-path razermapper/razermapper-gui/Cargo.toml
  </verify>
  <done>
- ashpd dependency present in Cargo.toml
- Version specified as 0.8
- default-features = false, features = ["wayland"] configured
- cargo check passes with no errors
  </done>
</task>

</tasks>

<verification>
1. cargo check passes for razermapper-gui
2. grep confirms ashpd = { version = "0.8", default-features = false, features = ["wayland"] }
3. No compilation errors related to missing dependencies
</verification>

<success_criteria>
1. ashpd crate added to razermapper-gui dependencies
2. Dependency configured with minimal features (wayland only)
3. GUI crate compiles successfully
4. Foundation laid for FocusTracker component in next plan
</success_criteria>

<output>
After completion, create `.planning/phases/13-wayland-portal-integration/13-01-SUMMARY.md`
</output>
