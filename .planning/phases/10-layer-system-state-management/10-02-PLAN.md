---
phase: 10-layer-system-state-management
plan: 02
type: execute
wave: 2
depends_on: [10-01]
files_modified:
  - razermapper/razermapperd/src/layer_manager.rs
autonomous: true
user_setup: []

must_haves:
  truths:
    - "Hold mode layers activate on key press and deactivate on key release"
    - "Hold layer activators are consumed (not forwarded to remap engine)"
    - "Active hold layers tracked separately from toggle layers"
    - "LayerManager::activate_hold_layer() adds to active_hold_layers HashSet"
  artifacts:
    - path: "razermapper/razermapperd/src/layer_manager.rs"
      provides: "hold layer activation tracking"
      contains: "activate_hold_layer"
      contains: "deactivate_hold_layer"
      contains: "active_hold_layers: HashSet<usize>"
  key_links:
    - from: "DeviceLayerState"
      to: "active_hold_layers"
      via: "HashSet insert/remove on key events"
      pattern: "active_hold_layers\\.insert|remove"
---

<objective>
Implement hold mode activation (layer active while modifier held)

Purpose: Enables layers that are active only while a modifier key is held down. This
is the standard layer activation mode (like Fn keys on keyboards). Hold layers
activate on press and deactivate on release.

Output: Hold layer activation methods in LayerManager with press/release tracking
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@razermapper/razermapperd/src/layer_manager.rs
@.planning/phases/10-layer-system-state-management/10-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Add hold layer activation tracking to DeviceLayerState</name>
  <files>razermapper/razermapperd/src/layer_manager.rs</files>
  <action>
Extend DeviceLayerState struct with:

1. Add field: `active_hold_layers: HashSet<usize>` - tracks currently held layers

2. Add method: `activate_hold_layer(&mut self, layer_id: usize)`
   - Inserts layer_id into active_hold_layers
   - No return value (always succeeds)

3. Add method: `deactivate_hold_layer(&mut self, layer_id: usize)`
   - Removes layer_id from active_hold_layers
   - Returns bool indicating if layer was active

4. Modify `get_effective_layer(&self) -> usize`
   - Union of active_hold_layers and active_toggle_layers (active_layers)
   - Return highest layer ID from combined set, or base_layer if empty

Key design: Hold layers tracked separately from toggle layers for clean
separation of concerns. Both contribute to effective layer calculation.
  </action>
  <verify>
grep -n "active_hold_layers" razermapper/razermapperd/src/layer_manager.rs
grep -n "fn activate_hold_layer" razermapper/razermapperd/src/layer_manager.rs
grep -n "fn deactivate_hold_layer" razermapper/razermapperd/src/layer_manager.rs
cargo check --manifest-path razermapper/razermapperd/Cargo.toml
  </verify>
  <done>
- active_hold_layers: HashSet<usize> field added to DeviceLayerState
- activate_hold_layer() method exists and inserts to HashSet
- deactivate_hold_layer() method exists and removes from HashSet
- get_effective_layer() considers active_hold_layers
- cargo check passes
  </done>
</task>

<task type="auto">
  <name>Add LayerManager hold activation methods</name>
  <files>razermapper/razermapperd/src/layer_manager.rs</files>
  <action>
Add to LayerManager impl:

1. `pub fn activate_hold_layer(&mut self, device_id: &str, layer_id: usize)`
   - Gets or creates device state
   - Calls device_state.activate_hold_layer(layer_id)

2. `pub fn deactivate_hold_layer(&mut self, device_id: &str, layer_id: usize)`
   - Gets device state (returns early if not exists)
   - Calls device_state.deactivate_hold_layer(layer_id)

These methods will be called from the event reader loop when detecting
layer activator key press/release events.

Return Result<(), String> for error handling (device not found, invalid layer).
  </action>
  <verify>
grep -n "pub fn activate_hold_layer" razermapper/razermapperd/src/layer_manager.rs
grep -n "pub fn deactivate_hold_layer" razermapper/razermapperd/src/layer_manager.rs
cargo check --manifest-path razermapper/razermapperd/Cargo.toml
  </verify>
  <done>
- activate_hold_layer() method exists on LayerManager
- deactivate_hold_layer() method exists on LayerManager
- Methods return Result for error handling
- cargo check passes
  </done>
</task>

<task type="auto">
  <name>Add hold mode unit tests</name>
  <files>razermapper/razermapperd/src/layer_manager.rs</files>
  <action>
Add to #[cfg(test)] mod tests:

1. test_activate_hold_layer() - Activate layer 1, verify active_hold_layers contains 1
2. test_deactivate_hold_layer() - Activate then deactivate, verify HashSet empty
3. test_hold_layer_affects_effective_layer() - Hold layer 2 active, get_effective_layer returns 2
4. test_multiple_hold_layers() - Hold layers 1 and 2, get_effective_layer returns 2 (highest)
5. test_hold_layer_cleanup() - Activate layer 1, deactivate layer 1, verify removed

Follow existing test pattern from plan 10-01.
  </action>
  <verify>
cargo test --manifest-path razermapper/razermapperd/Cargo.toml --lib layer_manager::test_hold
  </verify>
  <done>
- All hold layer tests pass
- test_hold_layer_affects_effective_layer verifies get_effective_layer priority
- test_multiple_hold_layers verifies highest ID wins
- cargo test passes
  </done>
</task>

</tasks>

<verification>
1. cargo check passes
2. cargo test --lib layer_manager passes all hold tests
3. activate_hold_layer adds to active_hold_layers HashSet
4. deactivate_hold_layer removes from active_hold_layers HashSet
5. get_effective_layer returns highest hold layer ID when active
6. get_effective_layer returns base_layer when no hold layers active
</verification>

<success_criteria>
1. Hold layer activation tracked in active_hold_layers HashSet
2. activate_hold_layer() adds layer to active set
3. deactivate_hold_layer() removes layer from active set
4. Hold layers contribute to effective layer calculation
5. Unit tests verify hold layer lifecycle
</success_criteria>

<output>
After completion, create `.planning/phases/10-layer-system-state-management/10-02-SUMMARY.md`
</output>
