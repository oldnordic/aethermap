---
phase: 10-layer-system-state-management
plan: 08
type: execute
wave: 5
depends_on: [10-07]
files_modified:
  - razermapper/razermapper-gui/src/ipc.rs
  - razermapper/razermapper-gui/src/gui.rs
autonomous: false
user_setup: []

must_haves:
  truths:
    - "GUI displays active layer for each device"
    - "GUI allows layer configuration (name, mode)"
    - "GUI provides layer activation buttons for toggle layers"
    - "Layer indicator updates in real-time as layers change"
  artifacts:
    - path: "razermapper/razermapper-gui/src/ipc.rs"
      provides: "IPC client methods for layer management"
      contains: "get_active_layer"
      contains: "set_layer_config"
      contains: "activate_layer"
      contains: "list_layers"
    - path: "razermapper/razermapper-gui/src/gui.rs"
      provides: "Layer display and configuration UI"
      contains: "layer_indicator"
      contains: "LayerSettings"
  key_links:
    - from: "gui.rs"
      to: "ipc.rs"
      via: "ipc_client.get_active_layer(), set_layer_config()"
      pattern: "get_active_layer.*device_id|set_layer_config"
    - from: "LayerSettings view"
      to: "Request::ListLayers"
      via: "Message::ListLayers"
      pattern: "Message::ListLayers"
---

<objective>
Implement GUI active layer display and configuration

Purpose: Extend GUI to show current active layer for each device and allow users
to configure layer settings (name, mode) and activate toggle layers. Provides
visual feedback for layer state and enables user control.

Output: GUI layer indicator display and layer configuration interface
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@razermapper/razermapper-gui/src/ipc.rs
@razermapper/razermapper-gui/src/gui.rs
@.planning/phases/10-layer-system-state-management/10-07-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Add layer management IPC client methods</name>
  <files>razermapper/razermapper-gui/src/ipc.rs</files>
  <action>
Add to razermapper-gui/src/ipc.rs IpcClient impl:

1. `pub async fn get_active_layer(&self, device_id: &str) -> Result<Option<usize>, Box<dyn std::error::Error>>`
   - Send Request::GetActiveLayer { device_id }
   - Parse Response::ActiveLayer { layer_id, ... }
   - Return Ok(Some(layer_id)) or Ok(None) if error

2. `pub async fn set_layer_config(&self, device_id: &str, layer_id: usize, name: String, mode: LayerMode) -> Result<(), Box<dyn std::error::Error>>`
   - Send Request::SetLayerConfig with LayerConfigInfo
   - Parse Response::LayerConfigured
   - Return Ok(()) or Err

3. `pub async fn activate_layer(&self, device_id: &str, layer_id: usize, mode: LayerMode) -> Result<(), Box<dyn std::error::Error>>`
   - Send Request::ActivateLayer { device_id, layer_id, mode }
   - Parse Response::LayerConfigured
   - Return Ok(()) or Err

4. `pub async fn list_layers(&self, device_id: &str) -> Result<Vec<LayerConfigInfo>, Box<dyn std::error::Error>>`
   - Send Request::ListLayers { device_id }
   - Parse Response::LayerList { layers }
   - Return Ok(layers) or Err

5. Import LayerMode, LayerConfigInfo from razermapper_common

Follow existing pattern for get_device_profiles(), activate_profile(), etc.
  </action>
  <verify>
grep -n "pub async fn get_active_layer" razermapper/razermapper-gui/src/ipc.rs
grep -n "pub async fn set_layer_config" razermapper/razermapper-gui/src/ipc.rs
grep -n "pub async fn activate_layer" razermapper/razermapper-gui/src/ipc.rs
grep -n "pub async fn list_layers" razermapper/razermapper-gui/src/ipc.rs
cargo check --manifest-path razermapper/razermapper-gui/Cargo.toml
  </verify>
  <done>
- get_active_layer() async method exists
- set_layer_config() async method exists
- activate_layer() async method exists
- list_layers() async method exists
- cargo check passes
  </done>
</task>

<task type="auto">
  <name>Add layer state to GUI model and messages</name>
  <files>razermapper/razermapper-gui/src/gui.rs</files>
  <action>
Add to gui.rs:

1. Extend Model struct:
```rust
pub struct Model {
    // ... existing fields ...
    active_layers: HashMap<String, usize>, // device_id -> active_layer_id
    layer_configs: HashMap<String, Vec<LayerConfigInfo>>, // device_id -> layers
}
```

2. Add Message enum variants:
```rust
pub enum Message {
    // ... existing variants ...
    LayerStateChanged(String, usize), // device_id, layer_id
    LayerConfigRequested(String), // device_id
    LayerActivateRequested(String, usize, LayerMode),
    LayerConfigUpdated(String, LayerConfigInfo),
}
```

3. Import types: `use razermapper_common::{LayerMode, LayerConfigInfo};`

4. Update Model::new() to initialize HashMaps

Follow existing pattern for device profiles and macros state.
  </action>
  <verify>
grep -n "active_layers: HashMap" razermapper/razermapper-gui/src/gui.rs
grep -n "LayerStateChanged" razermapper/razermapper-gui/src/gui.rs
grep -n "LayerActivateRequested" razermapper/razermapper-gui/src/gui.rs
cargo check --manifest-path razermapper/razermapper-gui/Cargo.toml
  </verify>
  <done>
- active_layers HashMap added to Model
- layer_configs HashMap added to Model
- Message variants: LayerStateChanged, LayerConfigRequested, LayerActivateRequested
- cargo check passes
  </done>
</task>

<task type="auto">
  <name>Add layer indicator view component</name>
  <files>razermapper/razermapper-gui/src/gui.rs</files>
  <action>
Add to gui.rs view function:

1. Create `fn layer_indicator(&self, device_id: &str) -> Element<Message>`
   - Display active layer name/ID for device
   - Use Text or Container with styling
   - Show "Layer N: {name}" format
   - Use Primary style for visibility

2. Create `fn layer_activation_buttons(&self, device_id: &str) -> Element<Message>`
   - Show button for each toggle layer
   - Button activates layer on press
   - Highlight active toggle layers with Secondary style
   - Only show layers with mode=Toggle

3. Integrate into device card view
   - Add layer indicator below device name
   - Add layer activation buttons in settings section

Follow existing GUI pattern for profile selector and device cards.
  </action>
  <verify>
grep -n "fn layer_indicator" razermapper/razermapper-gui/src/gui.rs
grep -n "fn layer_activation_buttons" razermapper/razermapper-gui/src/gui.rs
cargo check --manifest-path razermapper/razermapper-gui/Cargo.toml
  </verify>
  <done>
- layer_indicator() function exists
- layer_activation_buttons() function exists
- Functions return Element<Message> for iced integration
- cargo check passes
  </done>
</task>

<task type="auto">
  <name>Add layer settings view component</name>
  <files>razermapper/razermapper-gui/src/gui.rs</files>
  <action>
Add to gui.rs:

1. Create `fn layer_settings_view(&self, device_id: &str) -> Element<Message>`
   - Table/list of all layers for device
   - Show: Layer ID, Name, Mode (Hold/Toggle), Remap count
   - Edit button for each row to open layer config dialog
   - "Add Layer" button (up to max 8 layers)

2. Create `fn layer_config_dialog(&self) -> Option<Element<Message>>`
   - Modal dialog for editing layer config
   - Fields: Name (TextInput), Mode (Picklist: Hold/Toggle)
   - Save/Cancel buttons
   - Show when Model.layer_config_dialog is Some

3. Add Model field: `layer_config_dialog: Option<(String, usize, String, LayerMode)>`
   - (device_id, layer_id, name, mode)
   - Some when dialog open, None when closed

Follow existing dialog pattern (macro recording, profile settings).
  </action>
  <verify>
grep -n "fn layer_settings_view" razermapper/razermapper-gui/src/gui.rs
grep -n "fn layer_config_dialog" razermapper/razermapper-gui/src/gui.rs
grep -n "layer_config_dialog: Option" razermapper/razermapper-gui/src/gui.rs
cargo check --manifest-path razermapper/razermapper-gui/Cargo.toml
  </verify>
  <done>
- layer_settings_view() function exists
- layer_config_dialog() function exists
- layer_config_dialog field added to Model
- Dialog shows Name and Mode fields
- cargo check passes
  </done>
</task>

<task type="auto">
  <name>Add layer update handler to GUI</name>
  <files>razermapper/razermapper-gui/src/gui.rs</files>
  <action>
Add to gui.rs update() function:

1. Handle Message::LayerStateChanged(device_id, layer_id)
   - Update active_layers HashMap
   - Trigger UI refresh

2. Handle Message::LayerConfigRequested(device_id)
   - Call ipc_client.list_layers(device_id)
   - Update layer_configs HashMap

3. Handle Message::LayerActivateRequested(device_id, layer_id, mode)
   - Call ipc_client.activate_layer(device_id, layer_id, mode)
   - Update active_layers on success

4. Handle Message::LayerConfigUpdated(device_id, config)
   - Call ipc_client.set_layer_config()
   - Refresh layer list on success
   - Close dialog

5. Add periodic layer state refresh
   - Use subscription to poll layer state every 1-2 seconds
   - Or push updates from daemon (future enhancement)

Follow existing message handling pattern.
  </action>
  <verify>
grep -n "Message::LayerStateChanged" razermapper/razermapper-gui/src/gui.rs
grep -n "Message::LayerActivateRequested" razermapper/razermapper-gui/src/gui.rs
grep -n "Message::LayerConfigUpdated" razermapper/razermapper-gui/src/gui.rs
cargo check --manifest-path razermapper/razermapper-gui/Cargo.toml
  </verify>
  <done>
- LayerStateChanged message handler updates active_layers
- LayerActivateRequested handler calls activate_layer IPC
- LayerConfigUpdated handler calls set_layer_config IPC
- Message handling follows existing pattern
- cargo check passes
  </done>
</task>

<task type="checkpoint:human-verify">
  <what-built>Complete GUI layer display and configuration UI</what-built>
  <how-to-verify>
1. Build and run razermapper-gui
2. Connect to a running razermapperd daemon
3. Verify layer indicator shows "Layer 0: Base" for each device
4. Click "Layer Settings" button
5. Verify layer list shows all 3 default layers
6. Edit Layer 1: Set name to "Gaming", mode to "Toggle"
7. Save changes
8. Verify "Layer 1: Gaming" appears in layer list
9. Click layer activation button for Layer 1
10. Verify layer indicator updates to "Layer 1: Gaming"
11. Send IPC request to query active layer
12. Verify response returns layer_id=1
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues with the layer UI</resume-signal>
</task>

</tasks>

<verification>
1. cargo check passes for razermapper-gui
2. IPC client methods exist for all layer operations
3. GUI model tracks active_layers and layer_configs
4. Layer indicator view component exists
5. Layer activation buttons view exists
6. Layer settings view component exists
7. Layer config dialog exists
8. Message handlers for layer operations exist
</verification>

<success_criteria>
1. GUI displays active layer for each device
2. Layer settings accessible via button/menu
3. Layer config dialog allows name and mode editing
4. Layer activation buttons work for toggle layers
5. Layer indicator updates in real-time
6. User can verify layer changes visually in GUI
</success_criteria>

<output>
After completion, create `.planning/phases/10-layer-system-state-management/10-08-SUMMARY.md`
</output>
