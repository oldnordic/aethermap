---
phase: 01-core-remapping
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - aethermap/aethermapd/src/remap_engine.rs
  - aethermap/aethermapd/src/key_parser.rs
  - aethermap/aethermapd/src/lib.rs
autonomous: true

must_haves:
  truths:
    - "RemapEngine stores remaps as Arc<RwLock<HashMap<evdev::Key, evdev::Key>>> for O(1) lookup"
    - "KeyParser parses case-insensitive key names (KEY_A, key_a, Key_A all valid)"
    - "KeyParser expands friendly abbreviations (capslock -> KEY_CAPSLOCK, a -> KEY_A)"
    - "RemapEngine::remap() returns Option<evdev::Key> for input key code translation"
    - "RemapEngine::load_config() validates all key names at config load time (eager validation)"
    - "All key code mappings use evdev::Key enum type"
  artifacts:
    - path: "aethermap/aethermapd/src/key_parser.rs"
      provides: "Friendly key name to evdev::Key parsing"
      exports: ["KeyParser", "ParseError"]
      min_lines: 150
      covered_by: "Task 1: Create KeyParser with friendly name expansion"
    - path: "aethermap/aethermapd/src/remap_engine.rs"
      provides: "RemapEngine core translation logic"
      exports: ["RemapEngine", "RemapError"]
      min_lines: 100
      covered_by: "Task 2: Create RemapEngine with HashMap storage"
  key_links:
    - from: "aethermap/aethermapd/src/key_parser.rs"
      to: "aethermap/aethermapd/src/remap_engine.rs"
      via: "Arc<KeyParser> field in RemapEngine"
      pattern: "key_parser: Arc<KeyParser>"
    - from: "aethermap/aethermapd/src/lib.rs"
      to: "aethermap/aethermapd/src/remap_engine.rs"
      via: "pub mod remap_engine export"
      pattern: "pub mod remap_engine"
---

<objective>
Create RemapEngine and KeyParser components for translating input key codes to output key codes.

Purpose: This is the foundation of Phase 1 - the core translation engine that takes input key codes from grabbed devices and outputs remapped key codes. Without this component, no remapping can occur.

Output: Two new source files (key_parser.rs, remap_engine.rs) with a RemapEngine that can translate key codes and a KeyParser that handles friendly name expansion.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-core-remapping/01-CONTEXT.md
@.planning/phases/01-core-remapping/01-RESEARCH.md

# Existing codebase patterns to follow
@aethermap/aethermapd/src/lib.rs
@aethermap/aethermapd/src/macro_engine.rs
@aethermap/aethermapd/src/bin/test_grab.rs
@aethermap/aethermapd/src/injector.rs
</context>

<tasks>

<task type="auto">
  <name>Create KeyParser with friendly name expansion</name>
  <files>aethermap/aethermapd/src/key_parser.rs</files>
  <action>
    Create aethermap/aethermapd/src/key_parser.rs with:

    1. pub struct KeyParser with name_to_key: HashMap<String, evdev::Key> field
    2. KeyParser::new() constructor that builds the lookup table:
       - All standard KEY_* names from evdev::Key enum (use evdev::Key::from_str() or direct mapping)
       - Case-insensitive storage (normalize to lowercase)
       - Friendly abbreviations: esc, a-z, capslock, ctrl/leftctrl/rightctrl, shift/leftshift/rightshift, alt/leftalt/rightalt
       - Try KEY_ prefix auto-expansion for unrecognized names
    3. pub fn parse(&self, name: &str) -> Result<evdev::Key, ParseError> method:
       - Normalize input to lowercase
       - Direct lookup first
       - Try with KEY_ prefix if not found
       - Return ParseError::UnknownKey if not found
    4. ParseError enum with thiserror derive for error messages

    CRITICAL: Use evdev::Key enum for all key storage (per CONTEXT.md decision)

    DO NOT:
    - Store integer u16 codes directly
    - Create overly complex suggestion logic (that's Phase 3)

    REFERENCE patterns from:
    - injector.rs:89-136 for key code usage
    - evdev crate documentation for Key enum
  </action>
  <verify>
    cargo test -p aethermapd key_parser::tests 2>&1
    Verify test compilation and key parsing works
  </verify>
  <done>
    KeyParser struct exists with:
    - HashMap-based name-to-evdev::Key lookup
    - Case-insensitive parsing (KEY_A == key_a)
    - Friendly abbreviation support (a, capslock, ctrl)
    - KEY_ prefix auto-expansion
    - At least 30 common key codes mapped
  </done>
</task>

<task type="auto">
  <name>Create RemapEngine with HashMap storage</name>
  <files>aethermap/aethermapd/src/remap_engine.rs</files>
  <action>
    Create aethermap/aethermapd/src/remap_engine.rs with:

    1. pub struct RemapEngine with:
       - remaps: Arc<RwLock<HashMap<evdev::Key, evdev::Key>>> for O(1) lookup
       - key_parser: Arc<KeyParser> for name parsing

    2. impl RemapEngine with:
       - pub fn new() -> Self constructor
       - pub async fn load_config(&mut self, config: &HashMap<String, String>) -> Result<(), RemapError>:
         * Validate ALL key names first at config load time (eager validation)
         * Parse to evdev::Key codes using key_parser
         * Store in remaps HashMap
         * Log each remap with info! macro
       - pub async fn remap(&self, key_code: evdev::Key) -> Option<evdev::Key>:
         * Read remaps HashMap
         * Return cloned output key or None
       - pub async fn remap_count(&self) -> usize

    3. RemapError enum with thiserror:
       - InvalidKey variant with key name and source
       - Config variant for file errors

    4. Use tokio::sync::RwLock (same as macro_engine.rs pattern)

    CRITICAL: Validate ALL keys at config load time before storing any (eager validation per CONTEXT.md decision)

    REFERENCE patterns from:
    - macro_engine.rs:21-28 for Arc<RwLock<HashMap<>> pattern
    - config.rs:241-250 for YAML HashMap parsing pattern
  </action>
  <verify>
    cargo test -p aethermapd remap_engine::tests 2>&1
    Verify RemapEngine compiles and tests pass
  </verify>
  <done>
    RemapEngine struct exists with:
    - Arc<RwLock<HashMap<evdev::Key, evdev::Key>>> storage
    - load_config validates all keys at load time (eager validation)
    - remap() method returns Option<evdev::Key>
    - Proper error handling with RemapError enum
  </done>
</task>

<task type="auto">
  <name>Export remap_engine and key_parser modules</name>
  <files>aethermap/aethermapd/src/lib.rs</files>
  <action>
    Update aethermap/aethermapd/src/lib.rs:

    1. Add pub mod remap_engine; after pub mod config;
    2. Add pub mod key_parser; after pub mod device;

    DO NOT reorder existing modules - only add new ones
  </action>
  <verify>
    cargo check -p aethermapd 2>&1 | grep -E "(remap_engine|key_parser|error|warning)"
    Verify modules compile without errors
  </verify>
  <done>
    lib.rs exports:
    - pub mod remap_engine
    - pub mod key_parser
  </done>
</task>

</tasks>

<verification>
After all tasks complete, verify:

1. cargo check -p aethermapd compiles without errors
2. New modules are visible in aethermapd namespace
3. Run cargo test -p aethermapd to verify unit tests pass
</verification>

<success_criteria>
1. KeyParser can parse at least 30 common key names (letters, modifiers, numbers)
2. KeyParser handles case-insensitivity correctly
3. KeyParser expands friendly abbreviations (a -> KEY_A)
4. RemapEngine stores remaps as HashMap<evdev::Key, evdev::Key> in Arc<RwLock<>>
5. RemapEngine::load_config validates all keys at config load time (eager validation)
6. RemapEngine::remap returns correct evdev::Key output codes
7. Code follows existing patterns (RwLock usage, error handling)
8. No unwrap() calls on hot paths
</success_criteria>

<output>
After completion, create `.planning/phases/01-core-remapping/01-01-SUMMARY.md` with:
- Files created/modified
- Number of key codes supported
- Any deviations from research recommendations
- Test results
</output>
