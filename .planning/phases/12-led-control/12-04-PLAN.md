# Plan 12-04: LED Brightness Unit and Integration Tests

**Wave:** 3
**Depends on:** 12-01, 12-02, 12-03
**Gap closure:** false
**Autonomous:** true

## Overview

Add comprehensive tests for the LED brightness feature including unit tests for the IPC handlers, profile persistence, and GUI widget behavior.

## Context

Tests are located in:
- `razermapper/razermapperd/tests/` - Daemon integration tests
- `razermapper/razermapper-gui/tests/` - GUI tests

## Files Modified

- `razermapper/razermapperd/tests/ipc_tests.rs` - Add LED brightness IPC tests
- `razermapper/razermapperd/src/profile.rs` - Add profile brightness persistence tests
- `razermapper/razermapper-gui/src/gui.rs` - Add GUI brightness widget tests

## Tasks

<tasks>
1. Read `razermapper/razermapperd/tests/ipc_tests.rs` to understand test structure
2. Add LED brightness IPC tests:
   - `test_set_led_brightness_valid()` - Set brightness 50, verify response
   - `test_set_led_brightness_invalid()` - Set brightness 150, verify error
   - `test_get_led_brightness()` - Query current brightness
   - `test_led_brightness_persistence()` - Set brightness, restart daemon, verify restored
3. Add profile brightness persistence tests in `razermapper/razermapperd/src/profile.rs`:
   - `test_profile_with_brightness()` - Create profile with brightness, serialize
   - `test_profile_brightness_default()` - Profile without brightness uses 100
4. Add GUI widget tests in `razermapper/razermapper-gui/src/gui.rs`:
   - `test_led_slider_message_handling()` - Verify brightness messages update state
   - `test_led_slider_bounds()` - Verify slider enforces 0-100 range
   - Test that dragging slider updates display immediately
   - Test that releasing slider sends IPC request
5. Run all tests: `cargo test --package razermapperd --package razermapper-gui`
6. Verify test coverage for LED brightness feature is >80%
</tasks>

## Verification Criteria

- [ ] Unit tests for IPC SetLedBrightness handler
- [ ] Unit tests for IPC GetLedBrightness handler
- [ ] Integration test for brightness persistence across daemon restart
- [ ] Unit tests for profile brightness serialization/deserialization
- [ ] GUI widget tests for slider behavior
- [ ] All tests pass: `cargo test`
- [ ] Test coverage >80% for LED brightness code paths

## Must Haves

- Edge cases tested (0%, 100%, invalid values)
- Persistence verified across daemon restarts
- GUI state changes tested
- IPC communication tested end-to-end

## Success Definition

All tests pass:
```
running 8 tests
test ipc_tests::test_set_led_brightness_valid ... ok
test ipc_tests::test_set_led_brightness_invalid ... ok
test ipc_tests::test_get_led_brightness ... ok
test ipc_tests::test_led_brightness_persistence ... ok
test profile::test_profile_with_brightness ... ok
test profile::test_profile_brightness_default ... ok
test gui::test_led_slider_message_handling ... ok
test gui::test_led_slider_bounds ... ok

test result: ok. 8 passed; 0 failed
```

Coverage report shows:
- IPC handlers: 100%
- Profile persistence: 100%
- GUI widget: >85%
